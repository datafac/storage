using DTOMaker.Gentime;
using Microsoft.CodeAnalysis;

namespace DTOMaker.CSPoco
{
    internal sealed class CSPocoMember : TargetMember
    {
        public CSPocoMember(string name, Location location) : base(name, location) { }
    }
}
