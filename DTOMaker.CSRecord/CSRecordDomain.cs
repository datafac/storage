using DTOMaker.Gentime;
using Microsoft.CodeAnalysis;

namespace DTOMaker.CSRecord
{
    internal sealed class CSRecordDomain : TargetDomain
    {
        public CSRecordDomain(string name, Location location) : base(name, location) { }
    }
}
