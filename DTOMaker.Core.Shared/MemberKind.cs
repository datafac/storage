namespace DTOMaker.Gentime
{
    public enum MemberKind
    {
        Native,
        Entity,
        Binary,
        String,
        Vector, // todo replace with Rank (0=scalar, 1=vector, etc.)
    }
}